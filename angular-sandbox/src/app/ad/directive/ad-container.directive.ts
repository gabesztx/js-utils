import { Directive, ElementRef, HostListener } from '@angular/core';

@Directive({
  selector: '[appAdContainer]'
})
export class AdContainerDirective {
  constructor() {
    // this.element = el;
  }

}
