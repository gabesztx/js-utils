"use strict";
function identity0(arg) {
    return arg;
}
function identity1(arg) {
    return arg;
}
function identity2(arg) {
    return arg;
}
var outputString = identity2("myString");
var outputNumber = identity2(100);
var outputObject = identity2({ x: 0, y: 0 });
